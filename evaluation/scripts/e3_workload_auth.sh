#!/usr/bin/env bash
# Q2 — Workload CVM authentication / live binding B_w vs M={1,2,4,8,16}.
# Paper output: compact table (avg latency + throughput). Requires TDX fleet.
echo "Q2 Workload-CVM authentication requires the coauthor TDX provision/attest path."
echo "Wire launch-client / cvm-fleet-test per docs/integration/tdx_adapter_guide.md"
echo "See docs/evaluation/EVALUATION_QUESTIONS.md (Q2 table)."
exit 2
